{% extends "layout.html" %}
{% block body %}
	{% if session.user_info %}
  		<form action="{{ url_for('add_post') }}" method=post>
  	 		<div>{{ form.title.label }}: {{ form.title() }}</a></div>
  	 		<div>{{ form.content.label }}: {{ form.congent() }}</div>   
   			<div>{{ form.submit() }}</div>
   		</form>
	{% endif %}
{% endblock %}